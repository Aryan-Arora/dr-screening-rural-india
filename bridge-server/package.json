{
  "name": "dr-screening-bridge-server",
  "version": "0.1.0",
  "description": "Node/Express bridge between the React frontend and the MATLAB DR screening pipeline. Receives an uploaded fundus image, invokes MATLAB headlessly, waits for it to finish, and returns results as JSON per the PRD's fixed contract (docs/PRD_DR_Screening.docx, section 10).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  }
}
